import { ArgTypes, Canvas, Controls, Meta, Story } from '@storybook/blocks';
import * as NavMenuStories from './nav-menu.stories';

<Meta of={NavMenuStories} />

# Nav Menu

This is the nav menu description

<Canvas of={NavMenuStories.Default} />
<Controls of={NavMenuStories.Default} />
