import { ArgTypes, Canvas, Controls, Meta, Story } from '@storybook/blocks';
import * as CardStories from './card.stories';

<Meta of={CardStories} />

# Card

This is the card description

<Canvas of={CardStories.Accent} />
<Controls of={CardStories.Accent} />
